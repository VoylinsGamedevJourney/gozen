@abstract
class_name ModulePanel
extends Resource
## PCK files should have the same name as their folder in `res://module_panels/` and
## have an instance of this PCK resource with the name `module_panel.gd` inside of
## that folder.

# Use propagate_call to send info on the duration + current frame + ...

const MODULE_PANELS_PATH: String = "res://modules/"
